{
  "name": "tweeter-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run compile",
    "compile": "npm run clean && tsc",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sam": "npm run build && sam.cmd build && sam.cmd deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.946.0",
    "aws-lambda": "^1.0.7",
    "bcryptjs": "^3.0.3",
    "date-fns": "^3.3.1",
    "rimraf": "^6.1.0",
    "tweeter-shared": "file:../tweeter-shared",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/bcryptjs": "^2.4.6"
  }
}
